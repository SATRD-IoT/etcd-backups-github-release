apiVersion: v1
kind: Secret
metadata:
  name: etcd-github-backup
  namespace: etcd-backups
type: Opaque
stringData:
  GH_TOKEN: "replace-me"
  # Optional. Required only when ENABLE_AGE_ENCRYPTION=true.
  AGE_RECIPIENT: ""
